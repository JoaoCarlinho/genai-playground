#ELASTICSEARCH VARS
ELASTICSEARCH_HOST=https://es01:9200 # Hostname of the Elasticsearch service
ELASTIC_USER=elastic # Password for the 'elastic' user (at least 6 characters)
ELASTIC_PASSWORD=make_password # Password for the 'elastic' user (at least 6 characters)
ES_PORT=127.0.0.1:9200 # ES_PORT=9200 # Port to expose Elasticsearch HTTP API to the host
KIBANA_PASSWORD=make_password # Password for the 'kibana_system' user (at least 6 characters)
STACK_VERSION=9.0.0 # Version of Elastic products
CLUSTER_NAME=elasticsearch-cluster # Name of the Elasticsearch cluster

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE=basic
#LICENSE=trial

# Port to expose Kibana to the host
KIBANA_PORT=5601
#KIBANA_PORT=80

# Increase or decrease based on the available host memory (in bytes)
MEM_LIMIT=4g

# Project namespace (defaults to the current folder name if not set)
#COMPOSE_PROJECT_NAME=myproject